[
  {
    "id": "free-access",
    "claim": "Tilt Tag is free to play and needs no account.",
    "where": "landing page and README",
    "test": "npx playwright test --grep '@claim:free-access'",
    "sandbox": "fresh landing page; assert free and no-account copy, no account form, and no payment link"
  },
  {
    "id": "run-format",
    "claim": "A standard run starts with 90 seconds and three shields.",
    "where": "landing page, game HUD, and README",
    "test": "npx playwright test --grep '@claim:run-format'",
    "sandbox": "fresh /demo; assert the visible 1:30 timer and three-shield accessible state"
  },
  {
    "id": "core-rules",
    "claim": "Targets add points and hazards remove shields.",
    "where": "landing page and README",
    "test": "npx vitest run test/game.test.ts -t '@claim:core-rules'",
    "sandbox": "deterministic seeded game; place one target and one hazard on the player and assert score and shields"
  },
  {
    "id": "complete-run",
    "claim": "A run reaches a score summary.",
    "where": "game end screen and README",
    "test": "npx playwright test --grep '@claim:complete-run'",
    "sandbox": "fresh browser context at /demo?e2e=1; wait for the deterministic shortened test run to end"
  },
  {
    "id": "run-restart",
    "claim": "Play again resets the run.",
    "where": "game end screen and README",
    "test": "npx playwright test --grep '@claim:run-restart'",
    "sandbox": "demo end screen; press Play again and assert zero score, three shields, and playing state"
  },
  {
    "id": "settings-persist",
    "claim": "Control settings stay on this device.",
    "where": "landing page, privacy page, and README",
    "test": "npx playwright test --grep '@claim:settings-persist'",
    "sandbox": "fresh real-mode storage; set inversion, seated mode, and W A S D; reload and inspect controls"
  },
  {
    "id": "daily-layout",
    "claim": "Every player gets the same layout for the current UTC day.",
    "where": "play page and README",
    "test": "npx playwright test --grep '@claim:daily-layout'",
    "sandbox": "two fresh browser contexts; compare the displayed daily seed"
  },
  {
    "id": "local-privacy",
    "claim": "Scores and settings stay on this device.",
    "where": "landing page, privacy page, and README",
    "test": "npx playwright test --grep '@claim:local-privacy'",
    "sandbox": "fresh demo context; record every request and inspect storage keys during play"
  },
  {
    "id": "sensor-privacy",
    "claim": "Live tilt readings are not saved or sent; chosen calibration offsets stay on this device.",
    "where": "landing page, privacy page, and README",
    "test": "npx playwright test --grep '@claim:sensor-privacy'",
    "sandbox": "fresh demo context; dispatch distinct orientation values, inspect demo storage, and record outgoing requests"
  },
  {
    "id": "no-device-access",
    "claim": "The game does not request camera or location access.",
    "where": "landing page",
    "test": "npx playwright test --grep '@claim:no-device-access'",
    "sandbox": "fresh demo context with camera and location methods instrumented before page scripts run"
  },
  {
    "id": "offline-reload",
    "claim": "Works offline after the first visit.",
    "where": "README",
    "test": "npx playwright test --grep '@claim:offline-reload'",
    "sandbox": "own fresh browser context; load /demo online, wait for the service worker, set offline, then reload"
  },
  {
    "id": "frame-rate",
    "claim": "Average game-loop work stays under 20 ms with 4x CPU throttling.",
    "where": "README and handoff",
    "test": "npx playwright test --grep '@claim:frame-rate'",
    "sandbox": "Chromium sample run with 4x CPU throttling; measure the game simulation, render, and HUD work across 90 frames"
  },
  {
    "id": "tilt-control",
    "claim": "Phone tilt can be calibrated to move the magnet on supported devices.",
    "where": "landing page, play page, and README controls",
    "test": "npx playwright test --grep '@claim:tilt-control'",
    "sandbox": "fresh real-mode browser; center synthetic orientation values, then change tilt and assert the magnet moves"
  },
  {
    "id": "touch-control",
    "claim": "The on-screen movement pad controls the magnet with pointer or touch input.",
    "where": "landing page, play page, and README controls",
    "test": "npx playwright test --grep '@claim:touch-control'",
    "sandbox": "fresh demo; drag the visible movement pad and assert the magnet moves"
  },
  {
    "id": "keyboard-modes",
    "claim": "Arrow keys and W A S D both control the magnet.",
    "where": "landing page, play page, and README controls",
    "test": "npx playwright test --grep '@claim:keyboard-modes'",
    "sandbox": "fresh real-mode browser; move with ArrowRight, switch to W A S D, then move with KeyD"
  },
  {
    "id": "escape-pause",
    "claim": "Pressing Escape pauses an active run.",
    "where": "play page and README controls",
    "test": "npx playwright test --grep '@claim:escape-pause'",
    "sandbox": "fresh demo; press Escape and assert the paused dialog and paused board state"
  },
  {
    "id": "audio-mute-persistence",
    "claim": "Sound starts only after player input, and mute stays on this device.",
    "where": "README controls",
    "test": "npx playwright test --grep '@claim:audio-mute-persistence'",
    "sandbox": "fresh demo with an instrumented AudioContext; assert no audio context before input, one after input, then persist mute through reload"
  },
  {
    "id": "demo-reset-isolation",
    "claim": "Demo mode never reads real-game storage, and Reset demo removes only demo data.",
    "where": "README, demo banner, privacy page, and demo documentation",
    "test": "npx playwright test --grep '@claim:demo-reset-isolation'",
    "sandbox": "fresh origin storage with distinct real and demo values; assert demo uses sample values, reset removes a demo-only marker, and real values remain"
  },
  {
    "id": "no-third-party-resources",
    "claim": "The game does not load third-party scripts, fonts, or trackers.",
    "where": "privacy page and README",
    "test": "npx playwright test --grep '@claim:no-third-party-resources'",
    "sandbox": "fresh demo through play and pause; record every browser request and assert each resource is same-origin"
  }
]
